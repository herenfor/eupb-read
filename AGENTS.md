# AI 协作入口

本文件是 AI 编码工具的最短入口。项目规则保持模型无关；不要在不同模型的专用文件中复制完整规则。

## 工作目录边界

- 隔离开发目录：`<PROJECT_ROOT>/epub-reader`。AI 只能在这里修改项目。
- 真实源仓目录：`<PROJECT_ROOT>/eupb-read`。AI 只可读取和比较，不得修改、提交、同步或推送。
- 用户会在阶段完成后自行把确认过的改动同步到真实源仓并上传 GitHub。
- 用户口头提到的 `epub-read` 当前实际目录名是 `eupb-read`，不要自行创建第三份目录。

## 每次任务的阅读顺序

1. `docs/PROJECT_CONTEXT.md`
2. `docs/SOURCE_DELTA.md`
3. `CONTRIBUTING.md`
4. 与任务直接相关的文档和代码
5. 修 Bug 时再读 `docs/BUGFIX_LOG.md`；涉及渲染时必须读 `docs/rendering-layers.md`

## 核心约束

- 同一时间只有一个 AI 修改和测试代码，不为多写入者并行设计流程。
- 未经明确要求，不做顺手重构、批量格式化、依赖升级或目录搬迁。
- 测试在本地执行即可；本地测试书、临时脚本、截图和浏览器产物不属于源仓同步内容。
- 修改必须保持 `src/core`、`src/render`、`src/ui` 与 `src-tauri` 的职责边界。
- 新渲染兼容规则必须遵循 L1–L5 分层，并更新冲突台账。
- 每次产生可同步变更后，更新 `docs/SOURCE_DELTA.md`；修复 Bug 时同时更新 `docs/BUGFIX_LOG.md`。
- 不要代表用户执行从隔离目录到源仓的复制、Git 提交或远程推送。

## 完成条件

- 只修改任务范围内文件。
- 执行与风险相称的本地验证，并记录命令与结果。
- 更新当前任务记录和源仓差异记录。
- 清楚列出仍未验证、未解决或不建议同步的内容。
